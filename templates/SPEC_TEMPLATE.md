# <お題番号>-<お題名>: <お題タイトル>

> このファイルは `templates/SPEC_TEMPLATE.md` をコピーして作成してください。
>
> ディレクトリ名は3桁ゼロ埋めの番号プレフィックスを付けること（例: `katas/011-ebs-volume/`）。
> 番号は段位を表さない単純な連番。既存の最大値の次の番号を割り当てる（`find katas -maxdepth 1 -type d` で確認する）。
> 難易度は番号ではなく、末尾の「難易度」セクションのスコア・カテゴリで表現する（`docs/difficulty-score.md` 参照）。
> 採用してよいkataかどうか（個人の検証環境で再現できるか）は `docs/special-tier.md` を参照する。

## 背景・目的

<なぜこの構成を学ぶ必要があるのか、実務でどう使われる技術か、このお題を通じて何を身につけてほしいかを記載する>

## 要件（Must）

機械的にテスト可能な合格条件のみを列挙する。「適切に」「よしなに」など曖昧な表現は禁止。

- [ ]
- [ ]

## 制約（禁止事項）

- 過剰な権限付与の禁止（例: `0.0.0.0/0` への全ポート開放、`*` を使ったIAMポリシーなど）
- 使用可能なAWSサービスの範囲
- このお題の趣旨を損なう実装（範囲外のリソースで要件を満たす、など）の禁止

## 推奨（Should）

複数の正解が許容される領域。命名規則、タグ付け方針、変数化の粒度など。

-

## 受入テスト

`tests/acceptance.sh` で自動検証する項目。aws-cliやcurlなど、具体的なコマンドと期待結果を記載する。

| # | 検証内容 | コマンド例 | 期待結果 |
|---|---|---|---|
| 1 | | | |

## スコープ外

今回のお題では扱わない範囲を明記する（別のkataで扱う、今後のお題で扱う、など）。

-

## 難易度

`docs/difficulty-score.md` の計算式で算出し、以下の形式で明記する。模範解答（最小十分な構成）を基準に算出すること。
カテゴリは該当するものを列挙する（複数可、なければ「なし」）。

```
スコア: XX（S=?, R=?, 深さ=?, エッジ=?, T=?）
カテゴリ: カテゴリ名（複数可、なければ「なし」）
```
