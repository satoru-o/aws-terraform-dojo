# 003-cha / 001-multi-module: モジュール化・tfvars分離・リモートステート

> **Status: 準備中** — このお題のSPEC.mdはまだ確定していません。
> `templates/SPEC_TEMPLATE.md` に沿って、001-shiro・002-ki の内容が完了した後に詳細化します。

## 想定テーマ（下書き）

これまでのお題で単体実装してきた構成を、Terraformモジュールとして再構成し、
環境（dev/prodなど）ごとにtfvarsを分離し、ステートをリモート（S3 + DynamoDBロックなど）に
配置することを検証するお題を想定しています。

001-shiro・002-ki で習得した内容の上に、以下のような要素が加わる見込みです。

- 再利用可能なモジュールへの切り出し（`modules/` 配下）
- 環境ごとの `*.tfvars` 分離
- リモートステート（バックエンド設定）とステートロック
- モジュールの入出力（variables / outputs）設計

詳細な要件・制約・受入テストは、確定後に本ファイルへ追記します。
